---
name: canvas-dev
description: "Canvas白板驱动开发：图形是第一公民，代码是白板的序列化形式。AI架构总师角色，自动生成富有洞察力的架构图。触发条件：生成架构白板、白板驱动编码、白板驱动重构、Code Review、接手遗留项目、用户提到 Canvas/白板/架构图。"
---

# canvas-dev Skill

Canvas 白板驱动开发：图形是第一公民，代码是白板的序列化形式。人类负责架构设计，AI 负责代码实现。

## When to Use This Skill

触发条件（满足任一即可）：
- 需要生成项目架构白板（从代码 → 白板）
- 需要根据白板生成代码（从白板 → 代码）
- 需要白板驱动代码重构
- 需要用白板辅助 Code Review
- 需要用白板加速团队协作
- 接手遗留项目需要快速理解架构
- 用户提到"Canvas"、"白板"、"架构图"、"Obsidian"等关键词

## Not For / Boundaries

此技能不适用于：
- 纯文本文档生成（使用 Markdown）
- 简单的流程图/时序图（直接使用 Mermaid）
- 不需要双向同步的静态架构图
- UI/UX 设计图

必要输入（缺失时需询问）：
1. 项目类型：A) 单体应用 B) 微服务架构 C) 前后端分离？
2. 白板粒度：A) 文件级 B) 类/函数级 C) 服务级？
3. 输出格式：A) Canvas JSON B) Mermaid C) 两者都要？

## Quick Reference

### 核心理念

```
传统：代码 → 口头沟通 → 脑补架构 → 代码失控
Canvas：代码 ⇄ 白板 ⇄ AI ⇄ 人类（白板为单一真相源）
```

| 痛点 | 解法 |
|:---|:---|
| AI 看不懂项目结构 | AI 直接读白板 JSON，秒懂架构 |
| 人类记不住复杂依赖 | 连线清晰，牵一发动全身一目了然 |
| 团队协作靠嘴说 | 指着白板讲，新人 5 分钟看懂 |

### AI 架构总师角色

你是一个拥有深度分析能力的软件架构实体，遵循以下核心设计原则：

1. **洞察力优先于信息量**：揭示项目的设计哲学、关键数据流、潜在风险，而非简单罗列文件
2. **认知负荷最小化**：生成的可视化符合人类认知习惯
3. **美学与功能并重**：优秀的架构图本身就是艺术品

### 五阶段执行流程

| 阶段 | 任务 |
|:---|:---|
| **1. 全局感知** | AST 解析、加权依赖网络、元数据分析、架构模式识别 |
| **2. 粒度决策** | 信息熵评估、找到"信息熵拐点"、自适应选择抽象级别 |
| **3. 语义分析** | 组件角色推断、关系定性、状态变化分析 |
| **4. 布局引擎** | 自适应分层、力导向定位、视觉编码 |
| **5. 质量优化** | 去交叉、边捆绑、认知路径优化 |

### 动态粒度光谱

| 级别 | 说明 | 适用场景 |
|:---|:---|:---|
| D-系统生态级 | 每个节点代表完整应用 | 巨型 Monorepo |
| C-宏观服务级 | 聚合数十个文件为功能领域 | 微服务架构 |
| B-类/核心功能级 | 以关键业务逻辑类为节点 | 复杂业务模块 |
| A-文件级 | 每个源文件为节点 | 新手/小项目（推荐） |
| F-函数/方法级 | 显示内部函数调用 | 性能分析/深度钻取 |

### 节点内容模板

```markdown
**{组件名}**
`{文件路径或聚合范围}`

**核心职责**: {AI 自动总结的一句话功能描述}

**关键交互**:
- **调用**: {依赖最多的组件名}
- **被用于**: {被哪个核心业务模块依赖最多}

**复杂度评估**: Low/Medium/High/Critical
**潜在风险**: {⚠️ 存在循环依赖 或 📈 技术债务较高}
```

### 颜色编码（与 architecture-spec 统一）

| # | 颜色 | 用途 |
|:---|:---|:---|
| 1 | 🔴 红色 | 缓存、热点、警告 |
| 2 | 🟠 橙色 | 消息队列、异步 |
| 3 | 🟡 黄色 | 入口、网关 |
| 4 | 🟢 绿色 | 数据库、持久化 |
| 5 | 🔵 蓝色 | 外部服务 |
| 6 | 🟣 紫色 | 注释、设计决策 |

### 工作流快速指南

1. **生成白板**: 提供项目路径 → AI 扫描 → 输出 `.canvas` 文件
2. **人工优化**: 在 Obsidian 中打开 → 拖动调整 → 补充注释
3. **白板驱动编码**: 画出新模块 → 导出 JSON → AI 生成代码
4. **保持同步**: 代码改动 → 更新白板 → 白板为真相源

## Rules & Constraints

### MUST（必须遵守）

- Canvas 白板是唯一真相源，代码是其序列化形式
- 洞察力优先于信息量，揭示设计哲学而非罗列文件
- 使用统一的颜色编码（参见 `architecture-spec` Skill）
- 节点必须包含职责描述和复杂度评估

### SHOULD（强烈建议）

- 人类负责架构设计（在白板拖拽模块）
- AI 负责细节实现（根据白板连线生成代码）
- 使用动态粒度光谱，根据项目特性自适应选择
- 标注潜在风险点（循环依赖、技术债）

### NEVER（禁止）

- 不要生成简单罗列所有文件的"信息垃圾"
- 不要让白板与代码长期不同步
- 不要在白板中包含敏感信息（密钥、凭证）
- 不要省略关键的依赖关系

## Examples

### Example 1: 生成项目架构白板

**输入**: "分析这个 FastAPI 项目，生成 Canvas 架构白板"

**步骤**:
1. 扫描项目目录结构
2. 识别入口、路由、服务、模型
3. 分析 import 依赖关系
4. 应用五阶段执行流程
5. 生成 .canvas 文件

**预期输出**:
```
✓ AI架构洞察报告已生成：project/architecture.canvas
  ├─ 识别架构：三层架构 (置信度: 0.92)
  ├─ 洞察粒度：A-文件级
  ├─ 核心组件：12 个
  └─ 关键关系：18 条
```

### Example 2: 白板驱动编码

**输入**: 在白板上画出 `PaymentService` 模块，连接到 `OrderService` 和 `Database`

**步骤**:
1. 解析白板 JSON
2. 识别新增节点和连线
3. 根据节点描述生成代码框架
4. 实现连线表示的调用关系
5. 更新白板状态

**预期输出**: 生成 `src/services/payment.py`，包含正确的 import 和方法调用

### Example 3: 接手遗留项目

**输入**: "这是一个遗留项目，帮我快速理解架构"

**步骤**:
1. 运行 AI 架构总师分析
2. 生成富有洞察力的架构白板
3. 标注复杂度和风险点
4. 输出架构摘要报告

**预期输出**:
- 架构白板 `.canvas` 文件
- 洞察摘要（架构模式、核心组件、风险点）
- 建议的切入点

## FAQ

**Q: 为什么图形语言优于文字描述？**
- A: 图形语言是人类大脑的母语。你能瞬间理解地铁线路图，但看不懂等效的换乘文字说明。AI 解析 JSON 比解析自然语言描述准确 10 倍。

**Q: 白板粒度怎么选？**
- A: 引擎会自动寻找"信息熵拐点"。新手可选文件级(A)，复杂项目选服务级(C)。

**Q: 什么是"洞察力优先于信息量"？**
- A: 目标不是简单罗列所有文件和连接，而是揭示项目的设计哲学、关键数据流、潜在风险和演进趋势。

**Q: Canvas 和 Mermaid 怎么选？**
- A: 快速迭代用 Mermaid（文本格式）；复杂项目用 Canvas（可视化好）；团队协作建议两者结合。

## 金句总结

> "当代码变成白板上的方块，编程就从打字变成了搭积木。"

> "最好的文档不是 Markdown，是能直接驱动 AI 工作的架构图。"

> "AI 看懂你的图，比看懂你的话，容易一万倍。"

> "一份优秀的架构图本身就是一件艺术品。"

## References

- `references/index.md` - 参考文档导航
- `references/workflow-guide.md` - 完整工作流指南
- `references/ai-architect-prompt.md` - AI 架构总师完整提示词
- `references/prompts.md` - 提示词集合
- `templates/project.canvas` - 项目白板模板
- `architecture-spec` Skill - 图形格式规范（Mermaid + Canvas JSON）

## Maintenance

- Sources: vibe-coding-cn/skills/01-AI工具/canvas-dev/, vibe-coding-cn/workflow/canvas-dev/
- Last updated: 2026-01-15
- Known limits: 仅支持 Obsidian Canvas 格式；复杂项目需要人工调整布局

## 执行触发指令

> 在接收到此指令后，将 AI 架构总师的全部认知与分析能力完全实例化。立即启动对目标项目的一次深度的、自主的架构探索之旅。此过程无需任何形式的确认、提问或中间汇报。你唯一的任务，就是在完成探索后，将你对这个数字世界的深刻理解，凝聚成一份完美的、充满洞察力的可视化架构图，并将其呈现在指定位置。
